# OpenClaw Workspace 构建指南

> 来源：AI那些事儿（2026-02-21）
> 核心观点：Bot 不是笨，是"家"太乱了

---

## 核心原则

### Bot 每次醒来都是失忆的巨婴
它要搞清楚三个问题：
1. 我是谁？
2. 我该干嘛？
3. 我的工具在哪？

如果 workspace 太乱，光是"找路"就烧掉大半 context。

---

## 推荐结构

```
workspace/
├── AGENTS.md      ← 启动入口（目录地图，不是规则手册）
├── SOUL.md        ← 人格定义
├── USER.md        ← 关于用户
├── MEMORY.md      ← 长期记忆（只放纯金精华）
├── TOOLS.md       ← 工具笔记
├── HEARTBEAT.md   ← 定时任务
├── TODO.md        ← 待办
└── memory/
    ├── 2026-02-20.md    ← 每日 log
    ├── insights/        ← 压缩洞察
    └── lessons/         ← 踩坑记录
```

---

## 三个大坑

### 坑1：Skills 随地大小便
- **问题**：技能散落多处，Bot 改了 A 版本，跑的却是 B 版本
- **解法**：所有技能统一收容到 `~/clawd/skills/`

### 坑2：一个功能，三个备胎文件夹
- **问题**：x-poster/、twitter-bot/、x-tools/ 功能重叠
- **解法**：一个功能对应一个 skill，一个 skill 只能住一个专属文件夹

### 坑3：规矩写了一箩筐，Bot 根本不看
- **问题**：context 压缩时，干巴巴的规则被丢掉
- **解法**：把规则刻进"输出格式"里

**格式约束示例**：
```
所有对外发送内容，结尾必须带：Humanizer: ✅
修改核心配置前，必须先输出"三问自检"结果
```

---

## 高阶技巧

### 1. 入库前三问
往 AGENTS.md 塞东西前，先问：
- 删掉它会影响 99% 的日常操作吗？（不会就别放）
- 以后需要扩展吗？（需要就独立成 skill）
- 新人 5 分钟能看懂吗？

### 2. 记忆断舍离
- 每天把 log 压缩成 1/10
- 只保留决策和结论
- 推导过程全干掉

### 3. 记忆 TTL（保质期）
| 级别 | 存活时间 |
|------|----------|
| [P0] | 永不过期 |
| [P1] | 90 天 |
| [P2] | 30 天 |

### 4. Task Checklist
入口处放对照表：
```
写文章配图 → contentgen-skill
删库操作 → roundtable-skill 审批
```

---

## 底层逻辑四条

1. **入口清晰**：目录就是目录
2. **职责分离**：一个文件只干一件事
3. **记忆分层**：从原石提炼黄金
4. **规则落地**：用格式逼它执行，别指望自觉

---

## 对比我们当前结构

| 项目 | 我们现状 | 文章建议 | 差距 |
|------|----------|----------|------|
| MEMORY.md | ✅ 已精简 | 只放精华 | ✅ 已做到 |
| memory/ 分层 | ❌ 没有分层 | insights/ lessons/ | 待改进 |
| 记忆 TTL | ❌ 没有 | P0/P1/P2 分级 | 可考虑 |
| 格式约束 | ❌ 没有 | 输出格式强制 | 可借鉴 |
| Task Checklist | ❌ 没有 | 入口对照表 | 可考虑 |
| 入库三问 | ❌ 没有 | 严格筛选 | 可借鉴 |

---

*保存时间：2026-02-22*
