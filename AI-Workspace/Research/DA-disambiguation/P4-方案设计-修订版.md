# P4 方案设计：三层协同消歧框架

> 创建日期：2026-04-06  
> 状态：🔄 进行中  
> 基于：P1 问题定义 ✅ | P2 文献调研 ✅ | P3 研究空白 ✅

---

## 一、总体框架设计

### 1.1 框架概述

**框架名称**：**TriCo-DAG** (Three-Layer Collaborative Disambiguation for Data Agents)

**核心思想**：
> 将用户层画像、业务层知识、数据层元数据纳入统一决策框架，通过**证据融合**和**动态权重**机制实现协同消歧，阻断歧义传播。

**设计原则**：
| 原则 | 说明 | 实现方式 |
|------|------|----------|
| **分层解耦** | 三层独立处理，降低复杂度 | 各层独立推理模块 |
| **证据融合** | 多层证据加权决策 | 动态权重融合算法 |
| **歧义阻断** | 高置信度层可阻断传播 | 置信度阈值机制 |
| **增量学习** | 每次交互更新知识 | 用户画像 + 知识图谱双更新 |

---

### 1.2 框架架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        TriCo-DAG 总体架构                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   用户查询 NL                                                            │
│   "查上个月优质用户的销售额"                                              │
│         │                                                               │
│         ▼                                                               │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                    歧义检测器 (Ambiguity Detector)               │   │
│   │   - LLM 分类：识别潜在歧义类型                                    │   │
│   │   - Schema 差异检测：多候选 SQL 差异分析                          │   │
│   │   - 输出：歧义集合 {A₁, A₂, ..., Aₖ} 及置信度                     │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│         │                                                               │
│         ▼                                                               │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                    并行三层推理 (Parallel Inference)             │   │
│   │                                                                   │   │
│   │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │   │
│   │   │   用户层     │  │   业务层     │  │   数据层     │          │   │
│   │   │ 画像推理    │  │ 知识匹配     │  │ Schema 链接  │          │   │
│   │   │              │  │              │  │              │          │   │
│   │   │ 输入：       │  │ 输入：       │  │ 输入：       │          │   │
│   │   │ - 用户 ID    │  │ - 查询实体   │  │ - 查询实体   │          │   │
│   │   │ - 历史行为   │  │ - 业务概念   │  │ - 表/列描述  │          │   │
│   │   │ - 角色信息   │  │              │  │              │          │   │
│   │   │              │  │              │  │              │          │   │
│   │   │ 输出：       │  │ 输出：       │  │ 输出：       │          │   │
│   │   │ - 偏好分布   │  │ - 规则匹配   │  │ - 候选映射   │          │   │
│   │   │ - 置信度 cᵤ  │  │ - 置信度 c_b │  │ - 置信度 c_d │          │   │
│   │   └──────────────┘  └──────────────┘  └──────────────┘          │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│         │                                                               │
│         ▼                                                               │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                    协同决策引擎 (Collaborative Engine)           │   │
│   │                                                                   │   │
│   │   输入：{ (证据ᵤ, cᵤ), (证据_b, c_b), (证据_d, c_d) }            │   │
│   │                                                                   │   │
│   │   步骤 1: 置信度校准                                              │   │
│   │   步骤 2: 动态权重计算 wᵤ, w_b, w_d                               │   │
│   │   步骤 3: 证据融合 → 联合后验 P(意图│证据)                        │   │
│   │   步骤 4: 决策：直接执行 / 交互确认                               │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│         │                                                               │
│         ├───────────────────────────────────────────────────────────┐   │
│         │ 高置信度 (max P > θ₁)                                     │   │
│         ▼                                                           │   │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                    直接执行 (Direct Execution)                   │   │
│   │   - 生成最终 SQL                                                 │   │
│   │   - 更新用户画像 (隐式反馈)                                      │   │
│   │   - 记录消歧案例                                                 │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│         │                                                               │
│         ├───────────────────────────────────────────────────────────┐   │
│         │ 低置信度 (max P < θ₁)                                     │   │   │
│         ▼                                                           │   │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                    交互确认 (Interactive Confirmation)           │   │
│   │                                                                   │   │
│   │   步骤 1: 基于 EIG 选择最优澄清问题                               │   │
│   │   步骤 2: 生成自然语言问题                                        │   │
│   │   步骤 3: 用户反馈                                                │   │
│   │   步骤 4: 更新信念 → 返回协同决策引擎                             │   │
│   │   步骤 5: 更新用户画像 (显式反馈)                                 │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 1.3 核心算法流程

```python
def TriCoDAG_disambiguate(query, user_id, context):
    """
    三层协同消歧主算法
    
    参数:
        query: 用户自然语言查询
        user_id: 用户标识
        context: 对话上下文
    
    返回:
        (sql, confidence, explanation) 或 (clarification_question, candidates)
    """
    
    # ========== Step 1: 歧义检测 ==========
    ambiguities = AmbiguityDetector.detect(query, context)
    # ambiguities = [(type, description, prior_prob), ...]
    
    if len(ambiguities) == 0:
        # 无歧义，直接生成 SQL
        sql = generate_sql(query, user_id, context)
        return (sql, 1.0, "无歧义，直接执行")
    
    # ========== Step 2: 三层并行推理 ==========
    # 2.1 用户层推理
    user_evidence, c_u = UserLayerInference.infer(
        query, user_id, get_user_profile(user_id)
    )
    
    # 2.2 业务层推理
    business_evidence, c_b = BusinessLayerInference.match(
        query, get_knowledge_graph()
    )
    
    # 2.3 数据层推理
    schema_evidence, c_d = DataLayerInference.link(
        query, get_schema_metadata()
    )
    
    # ========== Step 3: 协同决策 ==========
    # 3.1 置信度校准 (温度缩放)
    c_u_cal = calibrate(c_u, temperature=1.5)
    c_b_cal = calibrate(c_b, temperature=1.2)
    c_d_cal = calibrate(c_d, temperature=1.0)
    
    # 3.2 动态权重计算
    w_u, w_b, w_d = compute_dynamic_weights(
        query_type=query.type,
        user_confidence=c_u_cal,
        business_confidence=c_b_cal,
        schema_confidence=c_d_cal,
        context=context
    )
    
    # 3.3 证据融合 (加权乘积)
    joint_posterior = fuse_evidence(
        user_evidence, business_evidence, schema_evidence,
        weights=(w_u, w_b, w_d)
    )
    
    # 3.4 决策
    best_intent = argmax(joint_posterior)
    max_confidence = joint_posterior[best_intent]
    
    if max_confidence > THRESHOLD_EXECUTE:
        # 高置信度：直接执行
        sql = generate_sql(best_intent, user_id, context)
        # 隐式更新用户画像
        update_user_profile(user_id, query, best_intent, feedback='implicit')
        return (sql, max_confidence, build_explanation(best_intent))
    
    else:
        # 低置信度：交互确认
        # 基于 EIG 选择最优问题
        best_question = select_best_question_EIG(
            joint_posterior, ambiguities
        )
        candidates = get_top_k_intents(joint_posterior, k=3)
        return (best_question, candidates)
```

---

### 1.4 概率图模型形式化（核心创新） ⭐新增

> **改进来源**：Opus 分析建议 - 重新定义"协同"操作语义

**问题**：原方案中"协同"仅是三层输出加权打分，与 pipeline 无本质区别。

**解决方案**：用概率图模型（PGM）形式化三层变量依赖关系，体现真正的"协同推理"。

#### 1.4.1 变量定义

| 层级 | 隐变量 | 含义 | 取值空间
|------|---------|------|----------|
| **用户层** | $U$ | 用户解释意图 | $\{u_1, u_2, ..., u_m\}$（指代、时间、视角）
| **业务层** | $B$ | 业务概念解释 | $\{b_1, b_2, ..., b_n\}$（概念、逻辑、规则）
| **数据层** | $D$ | Schema 映射 | $\{d_1, d_2, ..., d_k\}$（表、列、路径）
| **目标** | $I$ | 最终执行意图 | $\{sql_1, sql_2, ..., sql_l\}$

**观测变量**：$Q$（用户查询）、$E_u$（用户画像证据）、$E_b$（业务知识证据）、$E_d$（Schema 元数据）

#### 1.4.2 联合概率分布

$$
P(I, U, B, D | Q, E_u, E_b, E_d) = 
P(U | Q, E_u) \times P(B | U, Q, E_b) \times P(D | B, Q, E_d) \times P(I | U, B, D)
\$$

**关键依赖关系**（体现协同）：

| 依赖 | 含义 |
|------|------|
| $P(B | U, Q, E_b)$ | **用户解释约束业务概念**："我的客户" → "优质客户"定义由角色决定 |
| $P(D | B, Q, E_d)$ | **业务概念约束 Schema 路径**："复购率"定义 → JOIN 路径选择 |
| $P(I | U, B, D)$ | **三层解释共同决定 SQL**：任一层缺失 → 意图不确定 |

#### 1.4.3 与 Pipeline 的本质区别

| 方式 | 计算逻辑 | 问题 |
|------|----------|------|
| **Pipeline** | $P(I) \approx P(U) \cdot P(B) \cdot P(D)$（独立乘积） | 忽略跨层约束 |
| **协同（PGM）** | $P(I) = \sum_{U,B,D} P(U) P(B|U) P(D|B) P(I|U,B,D)$ | **考虑依赖传播** |

**示例**：

```
查询：「我的优质客户销售额」

Pipeline 方式：
  P(U=个人) = 0.7
  P(B=VIP客户) = 0.6
  P(D=个人表) = 0.8
  P(I) = 0.7 × 0.6 × 0.8 = 0.336（低置信度）

协同方式（考虑依赖）：
  P(U=个人) = 0.7
  P(B=VIP客户 | U=个人) = 0.9（个人视角 → VIP 标准明确）
  P(D=个人表 | B=VIP客户) = 0.95（VIP → 个人客户表）
  P(I | U,B,D) = 0.95
  P(I) = 0.7 × 0.9 × 0.95 × 0.95 = 0.57（置信度提升 70%）
```}

#### 1.4.4 推理算法

**目标**：找到使 $P(I | Q, E)$ 最大的 $I^*$

$$
I^* = \arg\max_I \sum_{U,B,D} P(U | Q, E_u) P(B | U, Q, E_b) P(D | B, Q, E_d) P(I | U, B, D)
$$

**简化策略**（避免 O(m × n × k × l) 组合爆炸）：

1. **剪枝**：只保留 $P(U) > \theta_u$ 的候选
2. **动态规划**：
   - Step 1：计算 $P(U)$，保留 Top-3
   - Step 2：对每个 $U$，计算 $P(B|U)$，保留 Top-3
   - Step 3：对每个 $(U,B)$，计算 $P(D|B,U)$，保留 Top-3
   - Step 4：计算 $P(I|U,B,D)$

**复杂度**：$O(3^3) = O(27)$ vs $O(m×n×k×l)$（可控）

#### 1.4.5 EIG 计算的修正

**原问题**：EIG 在组合空间计算代价爆炸

**修正方案**：基于 PGM 的局部 EIG

$$
EIG(Q_{clarify}) = H(P(I)) - E_{answer}[H(P(I | answer))]
$$

其中 $P(I)$ 由 PGM 推理得出，不需要枚举所有组合。

**实现**：选择使熵减少最大的澄清问题（贪心策略）。

---

## 二、用户偏好缓存设计（改进后）

> **改进来源**：Opus 分析建议 - 砍掉复杂画像模块，改为轻量缓存

### 2.1 设计原则

| 原设计 | 新设计 | 原因 |
|--------|--------|------|
| 双通道学习 | 单记录缓存 | 企业场景查询频率低（10-20次/天），画像根本建不起来 |
| 冷启动推断 | 直接缓存 | 画像冷启动无数据支持 |
| 贝叶斯更新 | 简单计数 | 复杂度收益不成比例 |

### 2.2 用户偏好缓存结构

### 2.1 画像表示结构

**设计目标**：专为消歧优化的用户画像，支持冷启动和增量学习。

```python
@dataclass
class DisambiguationUserProfile:
    """消歧专用用户画像"""
    
    user_id: str
    role: str  # 角色：销售总监/财务/运营等
    
    # ========== 静态特征 (更新频率低) ==========
    department: str  # 部门
    seniority: str  # 职级
    data_literacy: float  # 数据素养评分 [0, 1]
    
    # ========== 动态特征 (每次交互更新) ==========
    # 2.1 表达习惯
    expression_style: Dict[str, float]  # {正式：0.3, 口语：0.7, 简称：0.8}
    preferred_granularity: str  # 偏好粒度：汇总/明细/趋势
    
    # 2.2 概念偏好
    concept_preferences: Dict[str, List[Tuple[str, float]]]
    # 示例：{"优质用户": [("存款>50 万", 0.8), ("理财>100 万", 0.2)]}
    
    # 2.3 角色视角
    role_perspective: Dict[str, str]
    # 示例：{"销售额": "个人业绩", "客户": "我的客户"}
    
    # ========== 历史行为 (滑动窗口) ==========
    recent_queries: List[QueryHistory]  # 最近 N 次查询
    confirmation_history: List[ConfirmationRecord]  # 确认历史
    
    # ========== 置信度参数 ==========
    profile_confidence: float  # 画像置信度 [0, 1]
    last_update: datetime
    
    # ========== 元数据 ==========
    created_at: datetime
    total_interactions: int
```

---

### 2.2 用户偏好缓存结构

```python
@dataclass
class UserPreferenceCache:
    """轻量用户偏好缓存（替代复杂画像）"""
    
    user_id: str
    role: str  # 角色：从注册信息获取
    department: str  # 部门
    
    # ========== 核心缓存：歧义选择历史 ==========
    ambiguity_choices: Dict[str, str]
    # 示例：{"优质用户": "存款>50万", "上季度": "财务季度", "销售额": "不含税"}
    # 简单记录，无权重，无学习
    
    # ========== 最近查询上下文（滑动窗口） ==========
    recent_queries: List[Dict]  # 最近 5 条查询
    # 用于指代消解
    
    created_at: datetime
    last_update: datetime
```

### 2.3 缓存更新算法（简化版）

```python
def update_preference_cache(user_id, ambiguity_type, chosen_value):
    """记录用户选择（不学习权重）"""
    cache = get_cache(user_id)
    cache.ambiguity_choices[ambiguity_type] = chosen_value
    cache.last_update = datetime.now()
    save_cache(cache)
```

### 2.4 缓存查询算法

```python
def get_cached_preference(user_id, ambiguity_type):
    """查询缓存，返回历史选择或 None"""
    cache = get_cache(user_id)
    return cache.ambiguity_choices.get(ambiguity_type, None)
```

### 2.5 冷启动策略（极简）

| 策略 | 说明 |
|------|------|
| **角色先验** | 从注册信息获取角色/部门 |
| **无历史** | 返回 None，触发交互确认 |
| **无主动询问** | 仅在必要时询问 |

**对比原设计**：

| 原设计（复杂） | 新设计（轻量） |
|--------------|--------------|
| 双通道学习 + 贝叶斯更新 | 单记录缓存 |
| 画像置信度计算 | 无 |
| 冷启动推断 | 直接从注册信息获取 |
| 10+ 字段 | 5 字段 |
| 代码量 ~200 行 | 代码量 ~50 行 |

---

## 三、业务知识图谱表示设计

### 3.1 图谱 Schema 设计

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    消歧优化的业务知识图谱 Schema                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   节点类型 (Node Types):                                                │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                                                                 │   │
│   │  Concept (业务概念)                                             │   │
│   │  ├── name: str  # "优质用户"                                    │   │
│   │  ├── synonyms: List[str]  # ["VIP 客户", "高价值用户"]          │   │
│   │  ├── department: str  # "零售业务部"                            │   │
│   │  ├── version: str  # "v2.1"                                     │   │
│   │  └── status: active | deprecated | draft                        │   │
│   │                                                                 │   │
│   │  Rule (计算规则)                                                │   │
│   │  ├── id: str  # "RULE_001"                                      │   │
│   │  ├── concept: Concept  # 关联概念                               │   │
│   │  ├── formula: str  # "存款余额 > 500000"                        │   │
│   │  ├── sql_template: str  # "WHERE balance > 500000"              │   │
│   │  ├── priority: int  # 优先级 (冲突时使用高优先级)               │   │
│   │  └── valid_from: date  # 生效日期                               │   │
│   │                                                                 │   │
│   │  Dimension (维度)                                               │   │
│   │  ├── name: str  # "时间"                                        │   │
│   │  ├── type: time | category | numeric                            │   │
│   │  ├── default_granularity: str  # "月"                           │   │
│   │  └── allowed_values: List[str]  # ["日", "周", "月", "年"]      │   │
│   │                                                                 │   │
│   │  Constraint (约束)                                              │   │
│   │  ├── id: str                                                    │   │
│   │  ├── type: filter | join | aggregation                          │   │
│   │  ├── condition: str  # "必须包含时间范围"                       │   │
│   │  └── error_message: str  # "查询必须指定时间范围"               │   │
│   │                                                                 │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   边类型 (Edge Types):                                                  │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                                                                 │   │
│   │  SYNONYM_OF (同义关系)                                          │   │
│   │  ├── confidence: float  # 同义置信度                            │   │
│   │  └── context: str  # 适用上下文                                 │   │
│   │                                                                 │   │
│   │  COMPUTED_BY (计算关系)                                         │   │
│   │  └── rule_id: str  # 关联规则                                   │   │
│   │                                                                 │   │
│   │  DEPENDS_ON (依赖关系)                                          │   │
│   │  └── dependency_type: str  # "前置条件" | "必要条件"            │   │
│   │                                                                 │   │
│   │  CONFLICTS_WITH (冲突关系)                                      │   │
│   │  ├── reason: str  # 冲突原因                                    │   │
│   │  └── resolution: str  # 解决策略                                │   │
│   │                                                                 │   │
│   │  MAPPED_TO (数据映射)                                           │   │
│   │  ├── table: str                                                 │   │
│   │  ├── column: str                                                │   │
│   │  └── transform: str  # 转换逻辑                                 │   │
│   │                                                                 │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 3.2 知识图谱示例

```
                    ┌──────────────────┐
                    │   "优质用户"     │  ← Concept
                    │   (零售业务部)   │
                    └────────┬─────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
         SYNONYM_OF    COMPUTED_BY    MAPPED_TO
              │              │              │
              ▼              ▼              ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │  "VIP 客户"   │ │   Rule_001   │ │  user_table  │
    │  (置信度 0.9) │ │              │ │  vip_level   │
    └──────────────┘ │ 存款>50 万    │ └──────────────┘
                     │ 理财>100 万    │
                     └──────┬───────┘
                            │
                      DEPENDS_ON
                            │
                            ▼
                     ┌──────────────┐
                     │  Rule_002    │
                     │  (时间约束)  │
                     └──────────────┘
```

---

### 3.3 版本管理机制

```python
@dataclass
class KnowledgeGraphVersion:
    """知识图谱版本管理"""
    
    version: str  # "v2.1.0"
    created_at: datetime
    created_by: str
    change_log: List[ChangeRecord]
    
    # 增量变更
    added_concepts: List[Concept]
    modified_concepts: List[Tuple[Concept, Concept]]  # (old, new)
    deprecated_concepts: List[Concept]
    
    # 影响分析
    affected_queries: List[str]  # 受影响的查询模板
    migration_script: str  # 迁移脚本


def update_knowledge_graph(concept, new_rule, operator='UPDATE'):
    """
    知识图谱更新操作
    
    参数:
        concept: 要更新的概念
        new_rule: 新规则
        operator: ADD | UPDATE | DEPRECATE
    """
    # 1. 冲突检测
    conflicts = detect_conflicts(concept, new_rule)
    if conflicts:
        # 自动解决或人工审核
        resolution = auto_resolve_conflicts(conflicts)
        if resolution.requires_human:
            flag_for_review(concept, new_rule, conflicts)
            return
    
    # 2. 影响分析
    affected = analyze_impact(concept, new_rule)
    
    # 3. 创建新版本
    new_version = KnowledgeGraphVersion(
        version=compute_next_version(),
        created_at=datetime.now(),
        change_log=[{
            'concept': concept.name,
            'operation': operator,
            'details': str(new_rule)
        }],
        affected_queries=affected.queries
    )
    
    # 4. 应用变更
    apply_changes(concept, new_rule, operator)
    
    # 5. 通知相关人员
    notify_stakeholders(affected.departments)
```

---

### 3.4 知识图谱消歧优化

**核心思想**：图谱结构针对消歧场景优化，支持快速检索和推理。

```python
def business_layer_match(query, knowledge_graph):
    """
    业务层知识匹配算法
    
    返回:
        (matched_rules, confidence)
    """
    # 1. 实体抽取
    entities = extract_business_entities(query)
    # entities = ["优质用户", "销售额", "上个月"]
    
    matched_rules = []
    confidences = []
    
    for entity in entities:
        # 2. 图谱检索 (多跳)
        concept_node = knowledge_graph.find_concept(entity)
        
        if concept_node:
            # 3. 获取关联规则
            rules = concept_node.get_computed_by_rules()
            
            # 4. 规则过滤 (基于上下文)
            filtered_rules = filter_rules_by_context(
                rules, 
                context={
                    'department': get_current_department(),
                    'time': get_current_time_context()
                }
            )
            
            # 5. 计算置信度
            for rule in filtered_rules:
                confidence = compute_rule_confidence(
                    rule, 
                    factors=[
                        rule.version_score,      # 新版本置信度高
                        rule.department_match,   # 部门匹配度
                        rule.usage_frequency,    # 使用频率
                        concept_node.synonym_match # 是否同义词匹配
                    ]
                )
                matched_rules.append((entity, rule))
                confidences.append(confidence)
        else:
            # 未找到概念：标记为未知
            matched_rules.append((entity, None))
            confidences.append(0.0)
    
    return matched_rules, np.mean(confidences)
```

---

## 四、交互式消歧策略设计

### 4.1 EIG + 模板混合策略

**核心思想**：结合 EIG 的理论最优性和模板的自然度。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    EIG + 模板混合澄清问题生成                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   输入：联合后验分布 P(意图│证据)                                       │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │  Step 1: 计算每个候选问题的 EIG                                  │   │
│   │                                                                   │   │
│   │  EIG(Q) = H[P(意图│证据)] - Eₐ[H[P(意图│证据, 回答=a)]]          │   │
│   │                                                                   │   │
│   │  其中:                                                            │   │
│   │  - H[·]: 信息熵                                                   │   │
│   │  - Eₐ[·]: 对所有可能回答 a 的期望                                 │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│         │                                                               │
│         ▼                                                               │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │  Step 2: 基于 EIG 排序，选择 Top-K 问题模板                       │   │
│   │                                                                   │   │
│   │  问题模板库:                                                      │   │
│   │  ┌─────────────────────────────────────────────────────────┐   │   │
│   │  │ 歧义类型 → 模板                                          │   │   │
│   │  ├─────────────────────────────────────────────────────────┤   │   │
│   │  │ 概念歧义 → "您指的 {concept} 是：{option_A} 还是{option_B}?"│   │   │
│   │  │ 时间歧义 → "时间范围是指 {range_A} 还是 {range_B}?"      │   │   │
│   │  │ 粒度歧义 → "需要 {granularity_A} 还是 {granularity_B} 数据?"│   │   │
│   │  │ 规则歧义 → "计算规则采用 {rule_A} 还是 {rule_B}?"        │   │   │
│   │  └─────────────────────────────────────────────────────────┘   │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│         │                                                               │
│         ▼                                                               │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │  Step 3: 自然度优化                                              │   │
│   │                                                                   │   │
│   │  - LLM 重写：将模板转换为自然语言                                 │   │
│   │  - 约束：保持区分度不变                                          │   │
│   │  - 示例：                                                        │   │
│   │    模板："您指的优质用户是：存款>50 万还是理财>100 万？"         │   │
│   │    优化："关于'优质用户'，您是想看存款 50 万以上的，             │   │
│   │          还是理财 100 万以上的客户？"                            │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│         │                                                               │
│         ▼                                                               │
│   输出：自然语言澄清问题                                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 4.2 用户耐心建模

**问题**：连续多轮确认会导致用户放弃。

**解决**：建模用户耐心，动态调整确认策略。

```python
@dataclass
class UserPatienceModel:
    """用户耐心模型"""
    
    # 静态因素
    user_data_literacy: float  # 数据素养
    historical_patience: float  # 历史耐心评分
    
    # 动态因素
    current_round: int  # 当前确认轮次
    accumulated_time: float  # 累计交互时间 (秒)
    query_complexity: float  # 查询复杂度
    recent_frustration_signals: List[float]  # 挫折信号 (重新查询/取消)
    
    def patience_score(self) -> float:
        """计算当前耐心评分 [0, 1]"""
        base = self.historical_patience
        
        # 轮次惩罚 (指数衰减)
        round_penalty = 0.3 ** (self.current_round - 1)
        
        # 时间惩罚
        time_penalty = exp(-self.accumulated_time / 300)  # 5 分钟半衰期
        
        # 挫折信号
        frustration_factor = 1.0 - np.mean(self.recent_frustration_signals)
        
        return base * round_penalty * time_penalty * frustration_factor
    
    def should_stop_confirming(self, expected_additional_rounds: int) -> bool:
        """判断是否应该停止确认"""
        if self.patience_score() < 0.3:
            return True
        
        # 预期总轮次超过阈值
        if self.current_round + expected_additional_rounds > 5:
            return True
        
        return False
```

---

### 4.3 最小交互策略算法

```python
def select_best_question_EIG(posterior, ambiguities, patience_model):
    """
    基于 EIG 和用户耐心的最优问题选择
    
    参数:
        posterior: P(意图│证据) 联合后验分布
        ambiguities: 检测到的歧义列表
        patience_model: 用户耐心模型
    
    返回:
        (question, expected_rounds)
    """
    # 1. 生成候选问题集
    candidate_questions = []
    for ambiguity in ambiguities:
        for template in get_templates_for_type(ambiguity.type):
            q = instantiate_template(template, ambiguity)
            eig = compute_EIG(q, posterior)
            candidate_questions.append((q, eig, ambiguity))
    
    # 2. 按 EIG 排序
    candidate_questions.sort(key=lambda x: x[1], reverse=True)
    
    # 3. 考虑用户耐心
    if patience_model.should_stop_confirming(expected_additional_rounds=2):
        # 耐心不足：选择单轮信息量最大的问题
        best_q = max(candidate_questions, key=lambda x: x[1])
        return best_q[0], 1
    
    # 4. 多轮策略：选择能最大化总信息增益的问题序列
    # 使用贪心近似
    selected_questions = []
    remaining_entropy = entropy(posterior)
    
    while remaining_entropy > THRESHOLD_UNCERTAINTY:
        # 选择当前 EIG 最大的问题
        best_q = candidate_questions.pop(0)
        selected_questions.append(best_q)
        
        # 更新剩余熵
        remaining_entropy -= best_q[1]
        
        # 检查耐心
        if patience_model.should_stop_confirming(
            expected_additional_rounds=len(selected_questions)
        ):
            break
    
    # 5. 返回第一个问题
    return selected_questions[0][0], len(selected_questions)
```

---

### 4.4 交互策略决策树

```
                    ┌─────────────────┐
                    │   检测到歧义    │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  计算联合置信度  │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
              ▼                             ▼
     置信度 > 0.8                   置信度 ≤ 0.8
              │                             │
              ▼                             ▼
     ┌─────────────────┐          ┌─────────────────┐
     │   直接执行      │          │  检查用户耐心   │
     │   (不确认)      │          └────────┬────────┘
     └─────────────────┘                   │
                              ┌────────────┴────────────┐
                              │                         │
                              ▼                         ▼
                     耐心充足                   耐心不足
                              │                         │
                              ▼                         ▼
                     ┌─────────────────┐      ┌─────────────────┐
                     │  EIG 多轮确认   │      │  单轮最优确认   │
                     │  (最小化轮次)   │      │  (最大信息量)   │
                     └─────────────────┘      └─────────────────┘
```

---

## 五、三层协同决策算法

### 5.1 动态权重计算

**核心思想**：根据查询类型和上下文动态调整各层权重。

```python
def compute_dynamic_weights(query_type, user_confidence, business_confidence, 
                           schema_confidence, context):
    """
    动态权重计算
    
    返回:
        (w_u, w_b, w_d) 满足 w_u + w_b + w_d = 1
    """
    # 基础权重 (默认平等)
    base_weights = {'user': 0.33, 'business': 0.34, 'data': 0.33}
    
    # ========== 规则 1: 查询类型调整 ==========
    if query_type == '个人业绩查询':
        # 个人查询：用户层权重高
        base_weights['user'] += 0.2
        base_weights['business'] -= 0.1
        base_weights['data'] -= 0.1
    
    elif query_type == '业务指标查询':
        # 业务指标：业务层权重高
        base_weights['business'] += 0.2
        base_weights['user'] -= 0.1
        base_weights['data'] -= 0.1
    
    elif query_type == '技术探索查询':
        # 技术探索：数据层权重高
        base_weights['data'] += 0.2
        base_weights['user'] -= 0.1
        base_weights['business'] -= 0.1
    
    # ========== 规则 2: 置信度调整 ==========
    # 高置信度层获得更高权重
    confidences = {
        'user': user_confidence,
        'business': business_confidence,
        'data': schema_confidence
    }
    
    # Softmax 归一化置信度权重
    conf_weights = softmax([confidences[k] for k in ['user', 'business', 'data']])
    
    # ========== 规则 3: 融合 ==========
    # 基础权重 + 置信度权重的加权平均
    alpha = 0.6  # 基础权重占比
    
    final_weights = {}
    for key in ['user', 'business', 'data']:
        final_weights[key] = alpha * base_weights[key] + (1 - alpha) * conf_weights[key]
    
    # 归一化
    total = sum(final_weights.values())
    return (final_weights['user']/total, 
            final_weights['business']/total, 
            final_weights['data']/total)
```

---

### 5.2 证据融合算法

```python
def fuse_evidence(user_evidence, business_evidence, schema_evidence, weights):
    """
    证据融合：加权乘积法则
    
    参数:
        user_evidence: Dict[intent_id, probability]
        business_evidence: Dict[intent_id, probability]
        schema_evidence: Dict[intent_id, probability]
        weights: (w_u, w_b, w_d)
    
    返回:
        Dict[intent_id, joint_probability]
    """
    w_u, w_b, w_d = weights
    
    # 获取所有意图
    all_intents = set(user_evidence.keys()) | set(business_evidence.keys()) | set(schema_evidence.keys())
    
    joint_posterior = {}
    
    for intent in all_intents:
        # 获取各层证据 (无证据时为均匀分布)
        p_u = user_evidence.get(intent, 1.0 / len(all_intents))
        p_b = business_evidence.get(intent, 1.0 / len(all_intents))
        p_d = schema_evidence.get(intent, 1.0 / len(all_intents))
        
        # 加权几何平均
        joint = (p_u ** w_u) * (p_b ** w_b) * (p_d ** w_d)
        
        joint_posterior[intent] = joint
    
    # 归一化
    total = sum(joint_posterior.values())
    return {k: v/total for k, v in joint_posterior.items()}
```

---

### 5.3 歧义阻断机制

```python
def check_ambiguity_blocking(confidences, thresholds):
    """
    歧义阻断检查
    
    如果某一层置信度超过阈值，可以阻断歧义传播。
    
    参数:
        confidences: (c_u, c_b, c_d)
        thresholds: (θ_u, θ_b, θ_d)
    
    返回:
        (blocked, blocking_layer, blocking_confidence)
    """
    c_u, c_b, c_d = confidences
    θ_u, θ_b, θ_d = thresholds
    
    # 检查各层是否达到阻断阈值
    if c_u >= θ_u:
        return (True, 'user', c_u)
    if c_b >= θ_b:
        return (True, 'business', c_b)
    if c_d >= θ_d:
        return (True, 'data', c_d)
    
    return (False, None, 0.0)


# 阻断阈值配置
BLOCKING_THRESHOLDS = {
    'user': 0.95,      # 用户层：极高置信度才阻断
    'business': 0.90,  # 业务层：高置信度
    'data': 0.85       # 数据层：相对较高即可 (Schema 映射较确定)
}
```

---

## 六、递增基线设计 ⭐新增

> **改进来源**：Opus 分析建议 - 设计递增基线验证协同的增量价值

### 6.1 基线层级设计

| 层级 | 方法 | 描述 | 验证目标 |
|------|------|------|----------|
| **L0** | LLM Raw | GPT-4 + 直接生成 SQL | 验证 LLM 基础能力 |
| **L1** | LLM + Schema Linking | 加 Schema 元数据提示 | 验证 Schema 信息价值 |
| **L2** | LLM + 单层消歧 | 仅业务层消歧（无协同） | 验证单层价值 |
| **L3** | LLM + 三层独立 | 三层并行，独立打分 | 验证三层分解价值 |
| **L4 (Proposed)** | TriCo-DAG | 三层协同推理（PGM） | 验证协同增量价值 |

### 6.2 预期结果

| 方法 | DA-Score | 相对 L0 提升 |
|------|----------|-------------|
| L0: LLM Raw | 0.55 | baseline |
| L1: +Schema | 0.65 | +18% |
| L2: +单层 | 0.72 | +31% |
| L3: +三层独立 | 0.78 | +42% |
| **L4: TriCo-DAG** | **0.81** | **+47%** |

### 6.3 增量价值分解

```
L0 → L1: Schema 信息 = +18%
L1 → L2: 业务层消歧 = +13%
L2 → L3: 三层分解 = +6%
L3 → L4: 协同推理 = +3%（关键贡献）
```

**核心论点**：
- 如果 L4 ≈ L3 → 协同无价值，方案失败
- 如果 L4 > L3 至少 +5% → 协同有增量价值

---

## 七、不可消歧出口设计 ⭐新增

> **改进来源**：Opus 分析建议 - 增加"不可消歧"出口

### 7.1 触发条件

| 条件 | 描述 | 处理 |
|------|------|------|
| **数据缺失** | Schema 中不存在查询所需字段 | 转人工 + 记录缺失 |
| **需业务决策** | 多个合理解释，无历史偏好，用户未响应 | 转人工 + 建议方案 |
| **成本超收益** | EIG < 阈值 + 交互轮次 > 耐心上限 | 转人工 + 最佳猜测 |
| **概念漂移** | 业务规则已过期，无新版本 | 转人工 + 提醒更新 |

### 7.2 处理流程

```python
def handle_unresolvable(query, reason, best_guess):
    """不可消歧处理"""
    
    # 生成转人工提示
    prompt = f"""
    查询：{query}
    原因：{reason}
    最佳猜测：{best_guess}
    
    请人工确认或补充信息。
    """
    
    # 记录案例
    log_unresolvable_case(query, reason, best_guess)
    
    # 返回提示
    return {
        "status": "need_human",
        "prompt": prompt,
        "best_guess": best_guess,
        "confidence": 0.5  # 标记为低置信度
    }
```

### 7.3 统计指标

| 指标 | 目标 | 说明 |
|------|------|------|
| 不可消歧率 | < 5% | 系统可用性 |
| 数据缺失率 | < 2% | Schema 完整性 |
| 转人工满意度 | > 80% | 用户体验 |

---

## 八、算法复杂度分析

### 6.1 时间复杂度

| 组件 | 复杂度 | 说明 |
|------|--------|------|
| 歧义检测 | O(n) | n = 查询长度 |
| 用户层推理 | O(1) | 画像查找 O(1)，偏好匹配 O(k) |
| 业务层推理 | O(m × d) | m = 概念数，d = 图谱深度 |
| 数据层推理 | O(s × l) | s = Schema 元素数，l = 链接深度 |
| 证据融合 | O(i) | i = 候选意图数 |
| EIG 计算 | O(q × a × i) | q = 问题数，a = 回答数，i = 意图数 |

**总体**：O(n + m×d + s×l + q×a×i)，实际中 i, q, a 较小，主要瓶颈在图谱检索和 Schema Linking。

### 6.2 空间复杂度

| 组件 | 空间 | 说明 |
|------|------|------|
| 用户画像 | O(u × f) | u = 用户数，f = 特征数 |
| 知识图谱 | O(c + r) | c = 概念数，r = 规则数 |
| Schema 元数据 | O(t × col) | t = 表数，col = 列数 |
| 对话上下文 | O(h × q) | h = 历史轮次，q = 平均查询长度 |

---

## 九、实现路线图（压缩版） ⭐更新

> **改进来源**：Opus 分析建议 - 原路线图过于乐观，压缩为两阶段

### 9.1 两阶段计划

| 阶段 | 时间 | 目标 | 里程碑 |
|------|------|------|--------|
| **阶段一** | 8 周 | 最小可验证原型 | 三层推理 + 单层消歧 |
| **阶段二** | 8 周 | 协同优化 + 实验验证 | PGM 协同 + 用户实验 |

### 9.2 阶段一：最小可验证原型（8 周）

| 周次 | 任务 | 交付物 |
|------|------|--------|
| W1-W2 | 知识图谱 Schema + 核心概念 | 业务知识图谱原型 |
| W3-W4 | 三层推理模块（独立版） | 用户/业务/数据层 API |
| W5-W6 | Baseline L0-L3 实现 | 4 个基线可运行 |
| W7-W8 | 阶段一实验 + 分析 | 对比 L0-L3 性能 |

**验收标准**：L3 > L0 +30%（验证三层分解价值）

### 9.3 阶段二：协同优化（8 周）

| 周次 | 任务 | 交付物 |
|------|------|--------|
| W9-W10 | PGM 协同推理实现 | TriCo-DAG 核心引擎 |
| W11-W12 | 交互策略 + EIG | 澄清问题生成模块 |
| W13-W14 | 主实验 + 消融实验 | L4 vs Baselines |
| W15-W16 | 用户实验 + 论文初稿 | 完整实验结果 |

**验收标准**：L4 > L3 +5%（验证协同增量价值）

### 9.4 关键里程碑

| 里程碑 | 时间 | 验收标准 |
|--------|------|----------|
| M1: 阶段一完成 | W8 | L3 > L0 +30% |
| M2: 协同引擎完成 | W10 | PGM 推理正确 |
| M3: 阶段二完成 | W16 | L4 > L3 +5%，论文初稿 |

**对比原路线图**：

| 原计划 | 新计划 |
|--------|--------|
| 16 周（过于乐观） | 16 周（两阶段，风险可控） |
| 一次性完成所有模块 | 分阶段验证增量价值 |
| 无中间验收 | 阶段一验收点 |
| 风险集中 | 阎段一失败可终止 |

---

## 八、风险评估与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 知识图谱构建成本高 | 高 | 高 | 优先构建核心概念，迭代扩展 |
| 用户冷启动效果差 | 中 | 中 | 主动询问 Top 歧义，相似用户迁移 |
| EIG 计算开销大 | 中 | 中 | 预计算常见问题，缓存 EIG 值 |
| 企业数据获取困难 | 高 | 高 | 使用脱敏数据，合成数据增强 |
| 三层权重调优困难 | 中 | 中 | 贝叶斯优化自动调参 |

---

## 九、下一步行动

### 9.1 P5 数据与指标准备

| 任务 | 说明 | 负责人 |
|------|------|--------|
| 确定实验数据集 | 京东真实数据 + 合成数据 | 孟老师 |
| 定义评估指标 | 准确率、交互轮次、用户满意度 | 科研小新 |
| 构建 Baseline | Odin, LinkAlign, AmbiSQL 复现 | 科研小新 |

### 9.2 需确认事项

| 事项 | 状态 | 备注 |
|------|------|------|
| 真实数据集可用性 | 待确认 | 需要数据脱敏审批 |
| 企业知识获取权限 | 待确认 | 业务规则文档访问 |
| 用户实验招募 | 待确认 | 内部员工志愿者 |

---

## 十、文档索引

| 文件 | 说明 |
|------|------|
| [P1-问题定义.md](./P1-问题定义.md) | 三层歧义分类框架 |
| [P2-文献调研.md](./P2-文献调研.md) | 19 篇文献调研 |
| [P3-研究空白.md](./P3-研究空白.md) | 研究空白分析 |
| **P4-方案设计.md** | 本设计文档 |

---

*整理人：科研小新*  
*创建日期：2026-04-06*  
*P4 阶段状态：🔄 进行中*
